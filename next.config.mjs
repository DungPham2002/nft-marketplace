/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["beige-necessary-lobster-645.mypinata.cloud"],
    formats: ["image/webp"]
  }
};

export default nextConfig;
